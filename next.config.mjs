/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"benevolent-alligator-114.convex.cloud"}
        ]
    }
};

export default nextConfig;
